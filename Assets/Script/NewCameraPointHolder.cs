using UnityEngine;

public class NewCameraPointHolder : MonoBehaviour
{
    [SerializeField] public GameObject _newCameraPoint;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
